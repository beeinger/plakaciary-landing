import React from "react";

const Answer18 = React.forwardRef((props, ref) => (
  <div ref={ref}>
    We wszystkich innych kwestiach piszcie do nas na mejla:
    plakaciary@protonmail.com
  </div>
));

export default Answer18;
